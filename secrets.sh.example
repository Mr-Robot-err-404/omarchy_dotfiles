# Copy to secrets.sh and replace values as needed.
# Use a reusable, pre-authorized Tailscale auth key for unattended setup.
TAILSCALE_AUTHKEY=""

# Optional: exported into ~/.config/environment.d/90-personal-backup.conf.
# Add more API variables here and to API_ENV_VARS in install.sh.
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""
